'use client';

import { styled } from 'styled-components';

const AccountBlock = styled.main`
  min-height: 70vh;
  display: flex;
  align-items: center;
  justify-content: center;
`;
const Account = () => {
  return <AccountBlock>Account</AccountBlock>;
};

export { Account };
